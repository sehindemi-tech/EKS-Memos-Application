provider:
  name: aws
  aws:
    region: eu-west-2

sources:
  - traefik-proxy

policy: sync
txtOwnerId: eks-memos-application-external-dns
domainFilters:
  - memo.sehindemi.com

serviceAccount:
  name: external-dns